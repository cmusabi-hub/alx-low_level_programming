#include "3-calc.h"

/**
 * main - main function
 * @num1: first integer
 * @num2: second integer
 * @operator: operation to be executed
 *
 * Return: result of the arguments
 */

int main (int argc, char *argv[])
{

	int  num1, num2, sum;
	int (*f)(int, int);

	arg[1] = atoi(num1);
	argv[3] = atoi(num2);

	sum = f(num1, num2);

	return (0);
}

