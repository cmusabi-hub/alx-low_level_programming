#include "main.h"

/**
 * *_strcat - concatenates two strings
 * @dest: string one
 * @src:string two
 * return: returns dest
 */

char *_strcat(char *dest, char *src)
{
	int i;

	for (i = 0; src[i] = '\0'; i++ )
	{
		dest += src[i];
	}
	return (dest);
}
