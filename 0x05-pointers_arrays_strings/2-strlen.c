#include "main.h"

/**
 * _strlen - returns length of string
 * @s:the pointer to the string
 * Return: return the length of the string
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);
}
