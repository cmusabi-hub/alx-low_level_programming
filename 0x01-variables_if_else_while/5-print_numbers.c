#include <stdio.h>

/**
 * main - main fuction
 * Return: return 0 on success
 */

int main(void)
{
	int num  = 0;

	while (num < 10)
	{
		printf("%d", num);
		num++;
	}
	putchar('\n');
	return (0);
}

